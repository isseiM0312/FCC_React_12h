/* const num = [1,2,3,4,5]
    const num2 = num.map(function (value = 0)  {
        return value**2
    })
    console.log(num2) */

    /* key={item.id}
    img={item.coverImg}
    rating={item.stats.rating}
    reviewCount={item.stats.reviewCount}
    location={item.location}
    title={item.title}
    price={item.price}
    openSpots={item.openSpots} */